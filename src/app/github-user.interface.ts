import { Repository } from './repository.interface'; // Adjust the path as needed

export interface GitHubUser {
  repos: Repository[];
  // Include other properties you expect in the GitHub user objec
}
