// repository.interface.ts
export interface Repository {
  name: string;
  language: string; 
  description: string;// Add the 'language' property
  // Include other properties you expect in a GitHub repository
}
